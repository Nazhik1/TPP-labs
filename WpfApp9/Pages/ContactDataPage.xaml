<Page x:Class="MultiStepForm.Pages.ContactDataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Контактные данные">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Шаг 2. Контактные данные"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Электронная почта:"/>
            <TextBox x:Name="EmailBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Телефон:"/>
            <TextBox x:Name="PhoneBox"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Назад" Width="120" Height="35"
                    Margin="0,0,10,0" Click="Back_Click"/>
            <Button Content="Далее" Width="120" Height="35"
                    Click="Next_Click"/>
        </StackPanel>
    </Grid>
</Page>